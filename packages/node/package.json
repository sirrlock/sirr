{
  "name": "@sirr/sdk",
  "version": "0.1.0",
  "description": "Sirr (سر) — Node.js SDK and CLI for the ephemeral secret vault",
  "license": "BSL-1.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/SirrVault/sirr"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sirr": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@types/node": "^20.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "overrides": {
    "minimatch": "^10.2.1",
    "glob": "^11.0.0"
  }
}
